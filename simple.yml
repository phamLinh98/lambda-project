version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Installing dependencies..."
  build:
    commands:
      - echo "Zipping the source files..."
  post_build:
    commands:
      - echo "Build completed successfully."

